-- ============================================================
-- Seed: dev sample data (run only in development)
-- ============================================================

-- Insert a test admin user profile (must match a real auth.users row)
-- To promote a user to admin after they sign up:
-- update public.profiles set role = 'admin' where id = '<uuid>';

-- Sample permit requests (for UI testing with real user IDs)
-- Replace <user_uuid> with an actual auth.users.id after seeding users.
